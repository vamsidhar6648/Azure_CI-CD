trigger: none

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    curl -X POST "https://<ngrok-url>/job/Jenkins_triggers_ADO_Hybrid/buildWithParameters?TEST_TAG=@smoke&token=triggerado&cause=Triggered+from+ADO" \
    --user "vamsi:11c71c7c5cf6f713d7be26361f74f8194a"
  displayName: "Trigger Jenkins Job from ADO"
