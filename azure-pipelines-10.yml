trigger: none

pool:
  vmImage: 'ubuntu-latest'

parameters:
  - name: Module
    displayName: "Select Test"
    type: string
    default: "loginTest.spec.ts"
    values:
      - "loginTest.spec.ts"
      - "pim.spec.ts"

steps:
- script: |
    curl -v -X POST "https://3943-183-82-115-164.ngrok-free.app/job/Fresh_Jenkins_NodeWay_Trigger/buildWithParameters?MODULE=tests/$(Module)&token=triggerado" \
    --user "vamsi:118d82c1419ad2e50c4f27827acdde7a21"
  displayName: "Trigger Jenkins Git-based Pipeline"
